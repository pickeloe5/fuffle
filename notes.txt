 - You should be able to create dom just like you normally would
 - You should be able to easily bind parts of this dom to state
 - One state should do it for most applications
 - I think, in this case, the fake state is actually perfectly fine
 - I think there need to be two methods of bindings
 - One for simple reads
    - e.g. "Fuffle.text(state.title)"
 - And another for more complicated reads
    - e.g. "state.calculate(state => state.title + state.subtitle)"

 - Well, I separated out simple reads
 - I made a proxy that tracks what path is being read from state
 - And utility functions to bind that path in state to the dom
 - E.g. Fuffle.text(state.title)
 - But if "state" is this simple read proxy, what about the complex reads?
 - And what about writes, both simple and complex?
 - So maybe "stateWrapper.proxy.counter" is for simple reads.
 - And "stateWrapper.update(state => {state.counter++})" for writes.
 - And "stateWrapper.read(state => state.counter + 5)" for reads
 - We could make "readProxy" and "updateProxy" for simple writes
 - But reading "stateWrapper.proxy.counter" is not "state.counter"
 - I'm thinking I should only allow the complex reads and writes maybe